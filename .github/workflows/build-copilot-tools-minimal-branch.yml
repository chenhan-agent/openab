name: Build Copilot Tools Minimal Image

on:
  push:
    branches:
      - tools-minimal
    paths:
      - .github/workflows/build-copilot-tools-minimal-branch.yml
      - Dockerfile.copilot
      - Cargo.toml
      - Cargo.lock
      - crates/**
  workflow_dispatch:

concurrency:
  group: build-copilot-tools-minimal-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE_NAME: ghcr.io/chenhan-agent/openab-copilot-tools-minimal

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.copilot
          platforms: linux/amd64
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:tools-minimal
            ${{ env.IMAGE_NAME }}:tools-minimal-${{ github.sha }}
          cache-from: type=gha,scope=copilot-tools-minimal-linux-amd64
          cache-to: type=gha,scope=copilot-tools-minimal-linux-amd64,mode=max
